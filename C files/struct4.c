#include<stdio.h>
#include<stdlib.h>
#include<math.h>
strut point {
    int x,y;
};
float distance ( struct point p1, struct point p2)
{
    return sqrt(pow(p2x-p1x,2)+pow(p2y-p1y,2));

}
int main ()
{
    float 
    +
    
    
    
    
    
    ;
     printf("la distance est de %f",distance(p1,p2));
}
